# this mutation is here so that useUpdateCartMutation() is generated by fauna
mutation UpdateCart(
    $id: ID!
    $data: UpdateCartInput
) {
    # this calls the updateCart() function in the `type Mutation` section of the schema, which is in update_cart.ts
    updateCart(id: $id, data: $data) {
        ...Cart
    }
}

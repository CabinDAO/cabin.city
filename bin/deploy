# !/bin/bash

echo "Reading vars from .env"
source .env

forge script ./contracts/src/scripts/DeployContract.sol -vvvv --sig "deploy()" --broadcast --rpc-url $RPC_URL --private-key $DEPLOY_PRIVATE_KEY --verify --etherscan-api-key $ETHERSCAN_API_KEY
